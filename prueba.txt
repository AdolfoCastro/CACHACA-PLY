$Este es un comentario
Los comentarios pueden tener varias lineas
	y pueden estar indentados
ademas puede contener diferentes caracte!@(U@#D(H#2r423r 
$

$Este es un prototipo, sirven para dar de alta las funciones$
Prototype func Integer Algo1(Integer Algunos)

$Las variables que se definen fuera de 
funciones y fuera del main son GLOBALES$
Define: Integer aint
Define: Float bflt 
Define: Double cdbl
Define: Boolean dbool
Define: String  estr

$Esta es la definicion de una funcion$
func Integer Algo1(Integer a):
	$Las variables dentro de una funcion 
	pertenecen al scope de la funcion$
	Define: Integer aint
	Define: Float bflt
	Define: Double cdbl
	Define: Boolean dbool
	Define: String estr

func Integer fibonacci_recursivo(Integer n):
	if(n==0):
		return 0
	else:
		if(n==1):
			return 1
		else:
			return fibonacci(n-1) + fibonacci(n-2)
		end
	end

func Integer fibonacci_iterativo(Integer n):
	Define: Integer a
	Define: Integer b
	Define: Integer i
	a = 0
	b = 1
	for(i=1: i==n: i=i+1):
		a = b
		b = a + b
	end
	return a

$Este es el metodo main$
Start:
	Define: Array Integer arreglo[10]

	arreglo[0]=0
	arreglo[1]=1
	arreglo[2]=2
	arreglo[3]=3
	arreglo[4]=4

	Define: List Integer lista{1, 2, 3, 4, 5}

	Define: String varmain
	print("Entra al programa")
	
	#Asignaciones
	aint = 1
	bflt = 1.9
	cdbl = 3.7
	dbool = true
	estr = "Esto es un string"
	
	Algo1(aint)
	
	$Estatutos Condicionales$
	$If sencillo$
	if (aint > bflt && dbool==true || dbool==false):
		estr = "Es mayor"
	end
	$If Else$
	if (bflt == cdbl):
		estr = "Es igual" 
	else:
		estr = "No es igual"
	end
	$If Elif$
	if (estr == "No es igual"):
		estr = "Es diferente"
	else:
		if (estr == "Es igual"):
			estr = "No es diferente"
	end

	$IF ELSE IF ELSE$
	if (estr == "Es igual"):
		estr = "Son el mismo"
	else:
		if (estr == "Es diferente"):
			estr = "Cambio"
		else:
			estr = "Pues que paso?"
		end
	end

	switch:
		case aint<1:
			print("Es menor a 1")
		case aint<=1:
			print("Es menor o igual a 1")
		case aint==1:
			print("Es igual a 1")
		case aint>=1:
			print("Es mayor o igual a 1")
		case aint>1:
			print("Es mayor a 1")

	for(aint=1: aint==0: aint=aint+1):
		bflt = bflt + aint + 2
		print(aint)
	end

	while(aint<0):
		print(aint)
		aint=aint-1
	end
End
